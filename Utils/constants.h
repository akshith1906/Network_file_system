#ifndef CONST_H
#define CONST_H


#define NM_IP "127.0.0.1"  // IP address of the Naming Server
#define NM_PORT 5000       // Port of the Naming Server
#define BUFFER_SIZE 4096
#define HEADER_SIZE 256
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#endif